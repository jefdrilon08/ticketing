:css
  @import url("https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css");

.row
  .col
    - if @borrow_transactions.any?
      = render partial: "borrow_transactions/index_list"
    - else
      %p No transactions found

= render partial: "borrow_transactions/modals/new"
