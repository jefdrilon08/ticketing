= simple_form_for [:administration, membership_type] do |f|
  .row
    .col
      .form-group
        = f.input :name, input_html: { class: 'form-control' }
  %hr
  .btn-group
    %button{ type: "submit", class: "btn btn-primary" }
      .fa.fa-check
      Save
    - if membership_type.new_record?
      = link_to administration_membership_types_path, class: 'btn btn-danger' do
        .fa.fa-times
        Cancel
    - else
      = link_to administration_membership_type_path(membership_type), class: 'btn btn-danger' do
        .fa.fa-times
        Cancel
