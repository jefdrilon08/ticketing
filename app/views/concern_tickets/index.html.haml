= title 
.row
    .col 
        %h4 
            Concern Ticket
        .card 
            .card-body
            -# = render partial: "index_filter"
            = render partial: "index_list"
            -# = render "modal"

= render "concern_tickets/modals/new_concern"           
